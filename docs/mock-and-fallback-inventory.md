# Mock And Fallback Inventory

## Purpose

This document tracks every known v1 shortcut, fallback, mock, stub, or incomplete production substitute in MenuMaker.

Not every item here is bad. Some are intentional local-v1 constraints. The point is to keep the boundary honest: if something is not the final product architecture, it should be named, scoped, and replaceable.

## Current Policy

- LLM recipe generation is the primary recipe source.
- Deterministic nutrition calculation is the authority for calories and macros.
- Deterministic recipe templates are fallback only.
- Local-first runtime is allowed for v1, but schema and service boundaries should remain compatible with hosted sync later.
- User-facing behavior should not be patched for one example request. If a fix only works for "aguacate", it is wrong.

## Inventory

| Area | Current Shortcut | Status | Where | Why It Exists | Risk | Replacement Path |
| --- | --- | --- | --- | --- | --- | --- |
| Auth | Single local user stub with enforced owned-table `user_id` coverage and shared profile ownership guard for high-risk profile mutations | Intentional v1 local stub with hosted-readiness guard | `packages/db/src/env.ts`, `packages/db/src/appService.ts`, `packages/db/src/appActions.ts`, `packages/db/src/schemaOwnership.test.ts` | Local personal runtime on the MacBook Air M2 | No real multi-user session isolation; RLS/auth not exercised at runtime | Add hosted auth, real user identity, session mapping, and RLS policy tests while preserving existing `user_id` columns and ownership guard semantics |
| Sync | Hosted sync deferred, but generated outputs including AI cache are user-scoped and profile-scoped mutations assert local-user ownership before writing | Intentional v1 deferral with schema/service guard | ADR 0003, ADR 0007, checklist, `packages/db/src/appService.ts`, `packages/db/src/schemaOwnership.test.ts` | Personal local app first | Mobile/desktop state only works against the local server/database | Supabase/Postgres hosted deployment, account system, migrations, RLS, profile ownership validation |
| Nutrition Data | Deterministic scoring can now read imported source records, user-defined foods, USDA downloadable dataset foods, and user-confirmed aliases; seed foods remain the local baseline | Partially live source-backed with in-app source creation/import | `packages/nutrition/src/engine.ts`, `packages/db/src/appService.ts`, `packages/db/src/appActions.ts`, `packages/db/src/nutritionSourceImport.ts`, `packages/db/src/openFoodFactsImport.ts`, `packages/db/src/usdaFdcDownloadImport.ts`, `packages/db/src/seed.ts`, `apps/web/app/page.tsx`, `apps/mcp/src/server.ts` | Lets Open Food Facts barcode products, USDA FoodData Central downloadable JSON records, normalized BEDCA-style records, and user-entered per-100g foods become the scoring source without changing recipe/menu code | Coverage still depends on importing datasets, barcodes, or user-entered facts; BEDCA is still not available as an official local download/API | Add BEDCA adapter if/when a usable authorized export exists, dataset version metadata UI, and richer source-selection search |
| Nutrition Source Tables | Source schemas support idempotent imported records, user-defined foods, USDA downloaded files, and app/MCP/UI mutations; generation, edit, and calorie-adjustment scoring read them | Live import path plus Open Food Facts, USDA download, and custom-food actions | `packages/db/src/migrate.ts`, `packages/db/src/schema.ts`, `packages/db/src/seed.ts`, `packages/db/src/nutritionSourceImport.ts`, `packages/db/src/openFoodFactsImport.ts`, `packages/db/src/usdaFdcDownloadImport.ts`, `packages/db/src/appActions.ts`, `apps/web/app/page.tsx`, `apps/mcp/src/server.ts`, `packages/db/src/appService.ts` | Keeps the future data model in use rather than only mirrored from seed rows | Open Food Facts covers packaged products; USDA download covers generic international foods after local import; Spanish BEDCA adapter is still pending; custom foods depend on user-entered nutrition facts | Add BEDCA adapter when authorized, source search/filtering, and record dataset versions |
| Unit Conversion | Generic units exist, with food-specific conversions for common ambiguous servings | Improved deterministic fallback | `packages/nutrition/src/engine.ts`, `packages/nutrition/src/seedFoods.ts` | Lets recipes with `cucharada`, `unidad`, `taza`, or `rebanada` calculate more accurately | Foods without a specific serving conversion still fall back to estimated generic units | Expand per-food household conversions and prefer grams/ml in generated recipes |
| Recipe Generation | Deterministic recipe templates remain as fallback, controlled by persisted local fallback policy with env as default | Gated, surfaced, and app-configurable | `packages/nutrition/src/recipes.ts`, `packages/db/src/appService.ts`, `packages/db/src/migrate.ts`, `apps/web/app/page.tsx`, `apps/mcp/src/server.ts` | Keeps app usable if Codex OAuth is missing, stale, slow, or returns too few valid candidates | If enabled, templates can still mask provider-quality problems, but the setting is visible and mutable from Perfil/MCP | Keep fallback disabled during live-quality testing; reduce dependence as nutrition coverage and LLM retry/caching improve |
| Recipe Candidate Scoring | Week selection uses deterministic scoring and bounded repair, not a full optimizer | Pragmatic v1 heuristic | `packages/db/src/appService.ts`, `packages/db/src/weekPlanning.test.ts` | Avoids obvious repeats, daily calorie drift, banned-food leakage, unknown nutrition, and weak weekly protein before persistence | May miss better global menu combinations | Add a proper menu-level optimizer with candidate pools, constraints, beam search/local search, and richer scoring traces |
| Week Skeleton | Weekly generation now creates a first-class `WeekSkeleton` before recipe candidates | LLM-first with app-configurable local fallback | `packages/ai/src/planning.ts`, `packages/db/src/appService.ts`, ADR 0005 | Variety planning is passed into recipe generation and stored in generation settings | Deterministic fallback still exists for missing/failed provider and no user-facing skeleton editor exists | Add richer skeleton review/editing, async progress, and source-specific failure UI |
| Repair Loop | Weekly assembly has bounded deterministic repair for repetition, daily calorie drift, banned-food leakage, unknown nutrition, and low weekly protein | Partial live implementation | `packages/db/src/appService.ts`, `packages/db/src/caloriePlanner.ts`, `packages/db/src/weekPlanning.test.ts`, `apps/web/app/page.tsx` | Repairs obvious quality failures using the same validated candidate pools before persistence, records structured `RepairRequest` / `RepairResult` telemetry in the menu/job trace, and turns unresolved affected day/slot remediation into targeted regenerate actions | Not yet a full LLM repair controller with repair jobs or LLM-authored proposals | Add async repair jobs, retry logs, LLM repair proposals, and code-specific failure explanations when deterministic repair cannot fix a menu |
| Generation Jobs | Weekly generation, failed-job retry, target-remediation generation, regeneration previews, and long preview flows now use queued job rows plus explicit runners; Semana exposes enqueue-first weekly generation and regeneration preview controls, a local worker command and shared app/MCP action can process queued weekly/preview jobs outside the original web request, and app state/UI/MCP expose job status, retry, cancellation, worker-drain, completed preview apply, and manual execution fallback | Partial live implementation | `packages/db/src/appService.ts`, `packages/db/src/generationWorker.ts`, `packages/db/src/appActions.ts`, `apps/web/app/page.tsx`, `apps/mcp/src/server.ts`, `generation_jobs` table | Weekly generation input, retry input, target-remediation input, and preview input are serialized into a job, then a runner moves it queued -> running -> completed/failed/cancelled; regeneration mutations require a server-owned preview plan; users and agents can inspect real logs/failure codes and stop queued/running jobs; local v1 can run `npm run worker:generation` or `processQueuedGenerationJobs` to drain queued jobs | Some explicit run-now controls can still execute inside request/agent calls; applying a completed preview is synchronous after confirmation; cancellation is checkpoint-based for in-flight work, and there is no hosted worker queue or streaming transport | Move remaining explicit run-now controls and preview apply mutations to background workers or streaming execution, and add stronger worker-level cancellation |
| AI Cache | Successful structured recipe, week-skeleton, chat-planner, menu-chat, and generation-summary calls are cached under `user_id` | Partial live implementation | `packages/db/src/appService.ts`, `packages/db/src/migrate.ts`, `packages/db/src/schema.ts`, `packages/ai/src/generationSummary.ts`, `apps/web/app/api/chat/route.ts` | Reduces repeated recipe/skeleton/chat/summary calls, records generated outputs, and keeps cache rows compatible with hosted sync/RLS ownership | Repair LLM proposals still lack cache records because that live workflow does not exist yet; remediation is deterministic and persisted, not an AI cache user | Extend cache helper to future repair proposals |
| MCP Regeneration Previews | Regeneration and calorie previews return server-owned plans, and MCP can also queue/run/cancel preview jobs that persist the exact plan in `generation_jobs.result.plan` | Live for synchronous previews and preview-job runners; broader worker coverage still partial | `apps/mcp/src/server.ts`, `packages/db/src/appService.ts`, `packages/db/src/appActions.ts` | External agents can inspect or persist the exact plan before confirmation | Preview jobs still run on demand inside the request; no background worker or streaming progress | Move preview execution to async workers with progress logs and stronger cancellation |
| Chat Agent | Chat is command-surface plus limited planner, not a full tool-loop agent | Partial agentic implementation | `apps/web/app/api/chat/route.ts`, `packages/ai/src/chat.ts`, `packages/db/src/appActions.ts` | Keeps mutations behind typed actions and confirmation gates | Ambiguous multi-step requests may fall back to plain chat or shallow intents | Add full action registry planning loop, tool result memory, multi-step planning, state diffs, and explicit refusal/clarification policy |
| Chat Intent Parsing | Some chat commands use regex/deterministic heuristics before LLM planner | Intentional safety layer, incomplete coverage | `apps/web/app/api/chat/route.ts` | Reliable handling for high-risk commands like calorie changes | Coverage is uneven; phrasing outside heuristics may not trigger tools | Expand typed intents and tests; let LLM propose actions only through schemas, then deterministic validators decide |
| Local LLM Provider | Uses local Codex OAuth transport | Local-personal runtime dependency | `packages/ai/src/codexOAuth.ts` | Matches the user's current Mac setup and avoids browser tokens | Not a hosted production auth/key-management story | Keep provider boundary; add OpenAI API or hosted provider config with server-side secrets |
| Nutrition Confidence UX | Low-confidence/unknown ingredient failures can open guided ingredient mapping; the modal can search and filter the source-backed nutrition catalog, import Open Food Facts barcodes, import downloaded USDA FoodData Central JSON, and create a user-defined per-100g food inline; Perfil exposes the same source operations | Partial live implementation | `packages/nutrition/src/engine.ts`, `packages/db/src/appService.ts`, `packages/db/src/appActions.ts`, `apps/web/app/page.tsx`, `apps/mcp/src/server.ts` | v1 rejects bad candidates before UI, but the user can search imported/custom/seed foods, create missing local foods, save aliases, add packaged products, add USDA records, or add per-100g custom foods and retry through the same deterministic scorer from the failure context | Source filtering and import affordances exist, but confidence explanations are still terse and BEDCA remains pending | Add richer confidence explanations and BEDCA adapter when an authorized source is available |
| Failure UI | Generation failures are visible from persisted jobs with Spanish explanations, queued retry action, code-specific remediation metadata, guided target editing, guided preference relaxation, guided ingredient mapping, guided fallback-policy changes, and targeted repair-regeneration preview actions | Partial live implementation | `apps/web/app/page.tsx`, `packages/db/src/appService.ts`, `packages/db/src/appActions.ts`, `packages/db/src/remediation.ts`, `apps/mcp/src/server.ts` | Failed/running jobs appear on Semana/Historial, failed jobs include specific next steps, impossible targets can be edited and queued through typed actions, preference constraints can be relaxed before queueing retry, unknown aliases can be mapped before queueing retry, fallback can be enabled through a confirmation modal before queueing retry, and unresolved repair notices can queue a preview for the affected meal/day/week before the user applies the server-owned plan | Failure remediation generation is queue-owned, but applying a completed regeneration preview is still synchronous after confirmation | Move preview apply mutations to background workers with streaming progress |
| Data Reset/Admin | Profile deletion and full local reset are app-native with exact confirmation and export snapshots | Partial live implementation | `packages/db/src/appService.ts`, `packages/db/src/appActions.ts`, `apps/web/app/page.tsx`, `apps/mcp/src/server.ts` | Personal local app needs safe cleanup during testing and iteration | No restore/import flow yet; hosted account deletion semantics are not implemented | Add restore/import and hosted account deletion semantics |
| Evaluation | Weekly quality scenarios now cover skeleton coverage, repetition, calorie drift, banned-food leakage, unknown nutrition, and repair telemetry | Partial quality harness | `packages/db/src/weekPlanning.test.ts`, `packages/db/src/caloriePlanner.test.ts` | Hard constraints and obvious weekly quality regressions are covered before persistence | Deliciousness, edit-flow variety, and real LLM output quality still need fixtures | Add golden scenario tests and scored fixtures for meal edit, LLM candidates, and subjective recipe quality |

## Items That Are Not Mocks

These are intentional product constraints, not shortcuts:

- One serving per meal for one profile.
- Spanish default for new profiles.
- Adult-only macro suggestions.
- No pantry, grocery list, store pricing, or clinical nutrition in v1.
- Confirmation gates before week-wide or persistent changes.
- Locked meals and locked days being preserved instead of optimized around aggressively.

## Recently Removed Reward-Hacky Shortcut

The previous avocado-specific fallback recipes were removed from `packages/nutrition/src/recipes.ts`.

The edit flow should now use:

```text
user request -> LLM recipe candidates -> deterministic nutrition/macro/menu scoring -> best options -> user chooses scope -> mutation
```

If the local provider is unavailable or too few candidates pass validation, fallback templates can still fill the pool, but they must pass the same filtering and scoring rules.

## Priority Replacement Order

1. Add an official BEDCA adapter on top of the source-record import path.
2. Add richer confidence explanations inside remediation.
3. Extend AI cache and trace records to future repair proposals.
4. Expand repair-loop orchestration from bounded deterministic repair with telemetry to structured async repair jobs.
5. Move preview apply mutations to enqueue-first worker execution with streaming progress.
6. Move remaining run-now job execution from request-time runners to background async workers.
7. Add hosted auth/sync only when local product behavior is solid.
